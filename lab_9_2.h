#pragma once
#include <cmath>

// --- МАКРОСИ, ЯКІ ВИМАГАЮТЬСЯ ЗАВДАННЯМ ---

// Макрос для максимуму
#define MAX(a, b)   ((a) > (b) ? (a) : (b))

// Макрос для мінімуму
#define MIN(a, b)   ((a) < (b) ? (a) : (b))

// Макрос для модуля (абсолютного значення)
#define ABS(val)    (std::fabs(val))

// Макрос для піднесення до квадрату
#define SQR(x)      ((x) * (x))

// --- ФУНКЦІЯ ОБЧИСЛЕННЯ (ВАРІАНТ 14) ---
// Оголошення функції, яка буде обчислювати вираз
double calculate_w(double x, double z);